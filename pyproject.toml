[tool.poetry]
name = "bronski"
version = "0.1.0"
description = "Simple cron/beat service for Django"
authors = ["Curtis Maloney <curtis@tinbrain.net>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.7"
django = "^2.2"
croniter = "^0.3.31"

[tool.poetry.dev-dependencies]
flake8 = "^3.7.9"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
